# GitHub 仓库统计

快速获取指定 GitHub 用户的所有非 fork 仓库的详细信息，并以表格形式展示，便于分析。

## 特性

* 获取用户所有仓库（自动处理分页）
* 显示仓库名称、描述、主要语言、星标数、分支数、创建时间、最后更新时间
* 按星标数降序排序
* 点击仓库名称可直接跳转到 GitHub 页面
* 显示总计仓库数、总星标、总分叉

## 使用方法

1. 在浏览器中打开 `gitstats.html`。
2. 输入 GitHub 用户名（例如 `octocat`）。
3. 点击“Calculate”或按回车键。
4. 等待数据加载。
